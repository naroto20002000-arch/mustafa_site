# Mustafa: Do you love me? — tiny site

Open the site by double-clicking `index.html` or serving the folder.

Quick local serve (Python 3):

```bash
cd C:/Users/husse/Downloads/mustafa_site
python -m http.server 8000
# then open http://localhost:8000
```
